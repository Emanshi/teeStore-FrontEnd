import { RatingtooltipPipe } from './ratingtooltip.pipe';

describe('RatingtooltipPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingtooltipPipe();
    expect(pipe).toBeTruthy();
  });
});
